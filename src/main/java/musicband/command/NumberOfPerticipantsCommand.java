package musicband.command;

/**
 * Интерфейс, отвечающий за вывод суммы значений поля numberOfParticipants для всех элементов коллекции
 */
public interface NumberOfPerticipantsCommand {
    /**
     * Этот метод осуществляет вывод суммы значений поля numberOfParticipants для всех элементов коллекции
     */
    String addNumberOfParticipants();
}